# Cloudflare Pages local dev bindings (used by `wrangler pages dev`)
# Copy to `.dev.vars` for local Cloudflare dev server.

# ⚠️ SECURITY WARNING:
#   - Generate a secure secret: openssl rand -base64 32
#   - NEVER use 'dev-secret' in production
#   - Minimum 32 characters required
#   - Authentication will fail with internal server errors if not set properly
#   - Use different secrets for dev/test/prod environments

# NextAuth Configuration (for wrangler dev on port 8788)
# Note: NextAuth v5 supports both AUTH_* and NEXTAUTH_* naming
AUTH_SECRET=dev-secret
AUTH_URL=http://localhost:8788
AUTH_TRUST_HOST=true

# Optional: Google OAuth (set both together)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Feature flags and tuning
ANALYTICS_ENABLED=true
ANALYTICS_SINK=log
RATE_LIMIT_ENABLED=true
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_SECONDS=60
LOG_LEVEL=INFO
LOG_FORMAT=json
LOG_INCLUDE_TIMESTAMP=true
DATABASE_QUERY_TIMEOUT=5000
CACHE_DEFAULT_TTL=3600
ENABLE_PERFORMANCE_MONITORING=true
SLOW_QUERY_THRESHOLD_MS=1000

